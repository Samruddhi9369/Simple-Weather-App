
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Simple Weather Application</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css">
  </head>
  <body>
    <div class="container">
        <div class="row">
            <div class="col-md-6 m-auto">
                <h1 class="text-center display-4 my-4">Simple Weather Application</h1>
                <form action="/" method="post">
                    <div class="custom-file mb-3">
                        <div class="form-group">
                            <label city="usr">Enter City:</label>
                            <input name="city" id="city" type="text" class="form-control" placeholder="Enter a City" required>
                          </div>
                        <input type="submit" class="btn btn-primary btn-block" value="Get Weather">
                    </div>
                </form>
                <div class="card card-body mb-3 mt-4">
                    <% if(weather !== null){ %>
                    <p><%= weather %></p>
                    <% } %>

                    <% if(error !== null){ %>
                    <p><%= error %></p>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
  </body>
</html>